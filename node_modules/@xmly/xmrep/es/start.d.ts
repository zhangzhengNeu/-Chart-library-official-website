import { SdkConfig } from "./share/types";
/** 初始化入口，需指定appName及version */
export declare function start({ a: appName, c: customCommon, b: bussinessId, v: version, xl: enableXmLog, tid: thirdpartyId, ak: appKey, }: SdkConfig): boolean | undefined;
