import { WhiteList } from '../../share/types';
/**
 * 监控api， 入侵fetch/XMLHttpRequest
 */
export declare class RequestHook {
    private whiteList;
    private MAX_ALLOW_SIZE;
    private dataQueue;
    constructor({ whiteList, maxQueueLength, }: IFetch);
    start(): void;
    private hookFetch;
    private hookXhr;
    private report;
}
interface IFetch {
    whiteList: WhiteList;
    maxQueueLength: number;
}
export {};
