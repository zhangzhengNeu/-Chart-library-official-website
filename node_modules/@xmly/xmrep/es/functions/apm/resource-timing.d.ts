import { WhiteList } from "../../share/types";
interface Config {
    maxQueueLength: number;
    whiteList: WhiteList;
}
/**
 * 监控页面、资源（静态资源、api接口）网络情况
 */
export declare class ResourceTiming {
    private whiteList;
    private maxQueueLength;
    private offset;
    private errorList;
    private timings;
    constructor(config: Config);
    start(): void;
    private checkAvailable;
    private handleResourceError;
    private collect;
    private report;
    private getTimes;
    private formatTiming;
}
export {};
