import { isDev, loc } from '../../utils/env';
import getUUID from '../../share/uuid';
import getEnvInfo from './env';
export var url = loc.protocol + "//xdcs-collector." + (isDev() ? 'test.' : '') + "ximalaya.com/api/v1/statistics";
export var sid = getUUID();
export var env = getEnvInfo();
