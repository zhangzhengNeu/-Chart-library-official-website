import { SendItem } from './types';
declare class Report {
    static CACHE_KEY: string;
    private unloaded;
    private sendList;
    private pendingList;
    private failedList;
    private retryTimes;
    /** 业务数据的写关闭字段 */
    private stopKeys;
    /** 是否可以上报 */
    private canReport;
    constructor();
    /** 恢复缓存数据 */
    init(): void;
    private initLoop;
    send(data: SendItem, sync?: boolean): void;
    sendByNative(data: SendItem, sync?: boolean): void;
    flush(): void;
    checkSending(): boolean;
    private sendNext;
    private addSend;
    private pushSendList;
    private cacheSendList;
    /** 字节序列上传必须固定格式 */
    private report;
    private formatDataItem;
    private formatData;
}
/**
 * 数据上报
 * @param  data {any} 上报数据
 * @param  force {boolean} 是否强制立即发送
 */
export default function report(data: SendItem, force?: boolean): void;
export declare const reportObj: Report;
export {};
