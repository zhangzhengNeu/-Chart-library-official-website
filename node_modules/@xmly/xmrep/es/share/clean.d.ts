import { WhiteList } from '../share/types';
/**
 * 判断是否http(s)协议
 */
export declare const isHttp: (url: string) => boolean;
/**
 * 去除url查询参数、去除末尾/
 */
export declare const cleanUrl: (url: string) => string;
/**
 * 去除url中的数字，除非带有v|V|app
 */
export declare const cleanUrlNumber: (url: string) => string;
export declare const cleanUrlNumberEnd: (url: string) => string;
/**
 * 删除无用time数据，避免干扰(小于等于0或大于最大值)
 */
export declare const cleanTime: (data: {
    [key: string]: string | number;
}) => void;
/** 过滤在白名单内url（不上报） */
export declare const cleanWhiteList: (url: string, whiteList: WhiteList) => boolean;
