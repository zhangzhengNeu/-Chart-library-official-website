import { isDev } from '../utils/env';
export var logTimeLabel = typeof Symbol !== 'undefined' ? Symbol('logtime') : '__logTime';
/**  缓存数据生成时间*/
export var formatTimeData = function (item) {
    item[logTimeLabel] = +new Date;
    return item;
};
var custom;
export var setCustomCommon = function (v) {
    for (var key in v) {
        var ty = typeof v[key];
        if (ty !== 'string' && ty !== 'number' && ty !== 'undefined' && isDev()) {
            throw new Error("[XLog-ERR] custom props('" + key + "') type should be string or number, but got '" + ty + "'");
        }
    }
    custom = v;
};
export var getCustomCommon = function () {
    return custom;
};
