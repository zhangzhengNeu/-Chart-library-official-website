import { isString } from '../utils/obj';
var prevModule;
var prevPage;
var enterTime = +new Date();
export var PRE_PAGE_KEY = 'pk_pa';
export var PRE_MODULE_KEY = 'pk_mo';
/** ubt上报确定来源页 */
var data = {
    set prevModule(m) {
        prevModule = isString(m) ? m.trim() : undefined;
    },
    get prevModule() {
        return prevModule;
    },
    set prevPage(p) {
        prevPage = isString(p) ? p.trim() : undefined;
    },
    get prevPage() {
        return prevPage;
    },
    get time() {
        return enterTime;
    },
    set time(time) {
        enterTime = time;
    },
};
export default data;
