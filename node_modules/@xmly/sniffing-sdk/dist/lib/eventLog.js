"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var notSupport = typeof window === 'undefined' || typeof console === 'undefined';
exports.default = (function (prefix, isDebugger //全局注入打印日志的级别（多少）TODO: 暂时未用到
) {
    if (prefix === void 0) { prefix = 'Sniffing: '; }
    if (isDebugger === void 0) { isDebugger = false; }
    return function (text, level) {
        if (level === void 0) { level = 'info'; }
        if (notSupport || !isDebugger) {
            return;
        }
        var infoLog = console.log;
        var warnLog = console.warn;
        var errorLog = console.error;
        var info = typeof text === 'string' ? "" + prefix + text : text;
        if (level === 'error') {
            errorLog(info);
            return;
        }
        if (level === 'warn') {
            warnLog(info);
            return;
        }
        if (level === 'info') {
            infoLog(info);
        }
    };
});
//# sourceMappingURL=eventLog.js.map