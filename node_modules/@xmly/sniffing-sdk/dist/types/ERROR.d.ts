export declare enum ENVIRONMENT {
    REPEAT = "you are init sdk more than once, the new tracker map will be overwrided. Please check your code and use 'install'",
    NotSupport = "environment doesn't support exposure event use manualTrack method instead"
}
export declare enum NOTFOUND {
    NoPageEvent = "can't find any page events in current scope",
    FireWrongId = "can't find exposure event id",
    UnregistedId = "can't match event id in event list",
    RefNotReady = "can't find target in DOM",
    SniffingNotReady = "please init the sniffing first"
}
export declare enum EXCEPTION {
    Illegal = "target is illegal!",
    Repeated = "event id repeated!",
    ParamsParseError = "can't parse params! Please use stringified JSON object.",
    FireOnce = "el was fired once, expo event wouldn't bind to element -> "
}
export declare enum INFO {
    UseHide = "use pagehide",
    UseBeforeunload = "use beforeunload",
    UseUnmount = "use unmount"
}
