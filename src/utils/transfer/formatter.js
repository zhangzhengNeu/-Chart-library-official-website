/**
 *
 * formatter(type, config)
 * type: number | date
 * eg.
 * formatter('number', '0,0.00')  ----Numeral.js
 * formatter('date', 'YYYY-MM-DD')  ----echarts.format.formatTime
 *
 * formatter(func)
 *
 */
